{
  "name": "papermod",
  "private": true,
  "scripts": {
    "dev": "npx tailwindcss -i tailwind.css -o assets/styles/main.css",
    "watch": "npx tailwindcss -i tailwind.css -o assets/styles/main.css --watch",
    "build": "npx tailwindcss -i tailwind.css -o assets/styles/main.css --minify"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5",
    "autoprefixer": "^10.4",
    "tailwindcss": "^3.4"
  },
  "devDependencies": {
    "prettier": "^3.3",
    "prettier-plugin-tailwindcss": "^0.6"
  }
}
